import { type Component } from 'solid-js';

const Home: Component = () => {
  // const [data] = createResource('index', fetchJson<DataIndex[]>);

  return <div>Search for your stuff!</div>;
};

export default Home;
